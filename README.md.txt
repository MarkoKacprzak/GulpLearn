learn Gulp

run in cmd:

npm run-script init
gulp wiredep
npm run-script start


-------------
with injecting less to css
run in cmd:

npm run-script init
gulp inject 
npm run-script start

-----

automatic hosting app:

gulp serve-dev

try to change server/app.js (port) without restarting  gulp-nodemon restarts app 